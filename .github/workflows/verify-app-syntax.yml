name: Legacy APK Workflow (Manual Only)

on:
  workflow_dispatch:

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the primary Android workflow
        run: echo "APK builds are handled by android-cloud-build.yml"
